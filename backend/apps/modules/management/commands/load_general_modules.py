from django.core.management.base import BaseCommand
from apps.modules.models import Section, Module, ModuleStep, StepNote, EvaluationCriteria


class Command(BaseCommand):
    help = 'Загрузка общих обзорных модулей (без привязки к варианту экзамена)'

    def handle(self, *args, **options):
        self.stdout.write('Создание общих обзорных модулей...')

        section, _ = Section.objects.update_or_create(
            name='Общая подготовка к демо-экзамену',
            exam_variant=None,
            defaults={
                'description': (
                    'Базовые материалы по каждому типу модуля демо-экзамена КОД 09.02.07. '
                    'Здесь объясняется что нужно знать и уметь для выполнения каждого задания.'
                ),
                'order': 0,
                'is_active': True,
            }
        )

        self._create_module1(section)
        self._create_module2(section)
        self._create_module3(section)
        self._create_module4(section)
        self._create_module5(section)
        self._create_module6(section)

        self.stdout.write(self.style.SUCCESS('Общие модули загружены.'))

    def _create_module1(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='1',
            defaults={
                'name': 'Модуль 1: ER-диаграмма — основы',
                'description': 'Как проектировать базу данных: сущности, атрибуты, связи и нормализация до 3НФ',
                'instruction': (
                    'Этот модуль объясняет базовые принципы проектирования ER-диаграммы. '
                    'На экзамене вам нужно будет изучить документы заказчика и самостоятельно '
                    'спроектировать схему базы данных. '
                    'Максимальный балл: 8 (критерии 1.1 и 1.2).'
                ),
                'task_condition': (
                    'На экзамене: разработать ER-диаграмму производственной системы на основании '
                    'предоставленных документов заказчика (PDF задания, JSON с данными). '
                    'Диаграмма должна содержать все сущности, атрибуты и связи, нормализованные до 3НФ.'
                ),
                'duration': '60 минут',
                'max_score': 8,
                'order': 1,
            }
        )

        steps_data = [
            (1, 'Что такое ER-диаграмма и зачем она нужна',
             'ER-диаграмма (Entity-Relationship) — графическое представление структуры базы данных.\n\n'
             'Три основных элемента:\n\n'
             '**Сущность (Entity)** — объект реального мира, о котором хранится информация.\n'
             'Примеры: Клиент, Заказ, Товар, Сотрудник.\n'
             'На диаграмме изображается прямоугольником с именем в верхней части.\n\n'
             '**Атрибут** — характеристика сущности.\n'
             'Примеры: у Клиента — Имя, ИНН, Телефон; у Заказа — Дата, Статус.\n'
             'Первичный ключ (PK) — уникальный идентификатор записи, обычно Код_xxx INT IDENTITY.\n\n'
             '**Связь (Relationship)** — отношение между сущностями.\n'
             'Примеры: Клиент ОФОРМЛЯЕТ Заказ, Заказ СОДЕРЖИТ Товар.\n'
             'Реализуется через внешний ключ (FK) — поле в дочерней таблице, ссылающееся на PK родительской.',
             'Понимание трёх элементов ER-диаграммы: сущность, атрибут, связь',
             'info', 'Зачем это нужно',
             'ER-диаграмма — это чертёж БД перед её созданием. '
             'Хорошо спроектированная диаграмма = меньше ошибок при создании таблиц в SQL.'),

            (2, 'Кардинальность связей: 1:1, 1:N, N:M',
             'Кардинальность — количество записей одной сущности, связанных с записями другой.\n\n'
             '**1:1 (один к одному)**\n'
             'Каждая запись А связана ровно с одной записью Б.\n'
             'Пример: Заказ ↔ Расчёт_стоимости (у каждого заказа — один расчёт).\n'
             'Реализация: FK в таблице Б объявляется UNIQUE.\n\n'
             '**1:N (один ко многим)** — самый частый тип\n'
             'Одна запись А связана со многими записями Б.\n'
             'Пример: Клиент → Заказы (у одного клиента — много заказов).\n'
             'Реализация: FK (Код_клиента) в таблице Заказы.\n\n'
             '**N:M (многие ко многим)**\n'
             'Многие А связаны со многими Б.\n'
             'Пример: Заказ ↔ Товар (в одном заказе много товаров, один товар — в многих заказах).\n'
             'Реализация: промежуточная таблица (Спецификация) с FK на обе стороны.\n\n'
             '**Нотация Crow\'s Foot:**\n'
             '• || — обязательно один\n'
             '• O| — необязательно один\n'
             '• |< — обязательно много\n'
             '• O< — необязательно много',
             'Понимание типов связей и их реализации через FK',
             'important', 'На экзамене часто ошибаются',
             'N:M нельзя реализовать напрямую двумя FK. '
             'Всегда нужна промежуточная таблица (Спецификация, Материалы и т.п.).'),

            (3, 'Нормализация: 1НФ, 2НФ, 3НФ',
             'Нормализация устраняет избыточность данных и аномалии при вставке/удалении.\n\n'
             '**1НФ — Первая нормальная форма:**\n'
             '✓ Каждый атрибут содержит одно значение (атомарность)\n'
             '✓ Есть первичный ключ\n'
             '✗ Нарушение: поле "Товары" = "Стул, Стол, Шкаф" → разбить на отдельные строки\n\n'
             '**2НФ — Вторая нормальная форма:**\n'
             '✓ Выполняется 1НФ\n'
             '✓ Каждый неключевой атрибут зависит от ВСЕГО составного ключа\n'
             '✗ Нарушение: в таблице Спецификация(Код_заказа, Код_товара, Наименование_товара) — '
             'Наименование зависит только от Код_товара, а не от пары ключей → вынести в ТОВАР\n\n'
             '**3НФ — Третья нормальная форма:**\n'
             '✓ Выполняется 2НФ\n'
             '✓ Нет транзитивных зависимостей (атрибут зависит только от PK)\n'
             '✗ Нарушение: в ЗАКАЗ хранится Город_клиента — Город зависит от Клиента, не от Заказа → '
             'Город должен быть в таблице КЛИЕНТ\n\n'
             'Правило 3НФ: "каждый атрибут зависит от ключа, только от ключа и ничего кроме ключа".',
             'Умение проверить схему на соответствие 1НФ, 2НФ, 3НФ',
             'tip', 'Мнемоника для 3НФ',
             '"Зависит от ключа, только от ключа и ничего кроме ключа." '
             'Если атрибут можно перенести в другую таблицу — значит он не на своём месте.'),

            (4, 'Инструменты для рисования ER-диаграммы',
             'Выберите один из инструментов — все бесплатны и работают в браузере:\n\n'
             '**draw.io (app.diagrams.net)** — рекомендуется\n'
             '• Открыть https://app.diagrams.net\n'
             '• Create New Diagram → выбрать шаблон "Entity Relationship"\n'
             '• Перетаскивать блоки Entity из левой панели\n'
             '• Соединять стрелками с нотацией Crow\'s Foot\n'
             '• Экспорт: File → Export As → PNG или SVG\n\n'
             '**dbdiagram.io** — для тех кто умеет писать код\n'
             '• Открыть https://dbdiagram.io\n'
             '• Писать структуру таблиц в текстовом синтаксисе\n'
             '• Диаграмма строится автоматически справа\n'
             '• Экспорт в PNG или PDF\n\n'
             '**HTML + Mermaid** — если нужен файл для сдачи\n'
             '• Создать .html файл с тегом <script src="mermaid CDN">\n'
             '• Описать диаграмму в нотации erDiagram\n'
             '• Открыть в браузере — интерактивная диаграмма\n\n'
             'Все три инструмента позволяют отображать: сущности, атрибуты, PK/FK, кардинальность.',
             'Выбран инструмент, умение создать простую ER-диаграмму',
             'tip', 'Совет: начинайте с черновика',
             'Перед открытием инструмента нарисуйте черновик на бумаге: '
             'список сущностей и стрелки между ними. Так в 2 раза быстрее.'),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '1.1 Создание ER-диаграммы, отражающей предметную область до 3НФ',
                '0 — диаграмма отсутствует или не отражает описание предметной области\n'
                '1 — диаграмма частично соответствует описанию или имеет незначительные ошибки\n'
                '2 — диаграмма полностью соответствует описанию и требованиям задания',
                6,
            ),
            (
                '1.2 Выделение необходимых атрибутов из документации заказчика',
                '0 — атрибуты указаны менее чем на половину\n'
                '1 — атрибуты указаны на половину\n'
                '2 — атрибуты указаны полностью',
                2,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_module2(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='2',
            defaults={
                'name': 'Модуль 2: База данных MSSQL — основы',
                'description': 'Работа в SSMS: создание БД, таблиц, импорт данных из файлов заказчика',
                'instruction': (
                    'Этот модуль объясняет как работать с Microsoft SQL Server 2019 и SSMS. '
                    'На экзамене нужно создать БД по ER-диаграмме и импортировать тестовые данные. '
                    'Максимальный балл: 12 (критерии 2.1 и 2.2).'
                ),
                'task_condition': (
                    'На экзамене: создать базу данных в MSSQL 2019 по спроектированной ER-диаграмме. '
                    'Создать все таблицы с ограничениями (PK, FK, CHECK, DEFAULT). '
                    'Импортировать данные из предоставленного файла (JSON или Excel).'
                ),
                'duration': '90 минут',
                'max_score': 12,
                'order': 2,
            }
        )

        steps_data = [
            (1, 'Знакомство с SSMS: подключение к серверу',
             'SQL Server Management Studio (SSMS) — основной инструмент для работы с MSSQL.\n\n'
             '**Запуск и подключение:**\n'
             '1. Открыть SSMS (Microsoft SQL Server Management Studio)\n'
             '2. Появится диалог подключения:\n'
             '   • Server type: Database Engine\n'
             '   • Server name: . (точка = локальный сервер) или localhost\n'
             '   • Authentication: Windows Authentication\n'
             '3. Нажать Connect\n\n'
             '**Интерфейс SSMS:**\n'
             '• **Object Explorer** (слева) — дерево объектов: Databases, Tables, Views\n'
             '• **Query Editor** (по центру) — текстовый редактор SQL-запросов\n'
             '• **Results pane** (внизу) — результаты выполнения запросов\n\n'
             '**Горячие клавиши:**\n'
             '• F5 или Ctrl+E — выполнить запрос (или выделенный фрагмент)\n'
             '• Ctrl+N — новое окно запроса\n'
             '• Ctrl+S — сохранить файл .sql',
             'Успешное подключение к SQL Server, виден Object Explorer',
             'warning', 'Если сервер не запускается',
             'Открыть services.msc (Win+R → services.msc) → найти "SQL Server (MSSQLSERVER)" '
             'или "SQL Server (SQLEXPRESS)" → нажать Запустить.'),

            (2, 'Типы данных MSSQL: выбрать правильный',
             'Правильный выбор типа данных экономит место и предотвращает ошибки.\n\n'
             '**Текст:**\n'
             '| Тип | Применение |\n'
             '|---|---|\n'
             '| NVARCHAR(n) | Строки с кириллицей, переменной длины (до n символов) |\n'
             '| NCHAR(n) | Строки фиксированной длины (коды, артикулы) |\n'
             '| NVARCHAR(MAX) | Очень длинный текст (описания, заметки) |\n\n'
             '**Числа:**\n'
             '| Тип | Применение |\n'
             '|---|---|\n'
             '| INT | Целые числа, счётчики, идентификаторы |\n'
             '| DECIMAL(10,2) | Деньги, цены (10 цифр, 2 после запятой) |\n'
             '| BIT | Флаг: 0/1 (да/нет, заблокирован/нет) |\n\n'
             '**Даты:**\n'
             '| Тип | Применение |\n'
             '|---|---|\n'
             '| DATE | Только дата: 2025-01-15 |\n'
             '| DATETIME | Дата + время: 2025-01-15 14:30:00 |\n\n'
             '**Важно:** для кириллических данных использовать N-префикс при вставке:\n'
             '`INSERT INTO ... VALUES (N\'Иванов Иван\', ...)`\n'
             'и NVARCHAR/NCHAR для хранения — иначе символы превратятся в ????.',
             'Понимание основных типов данных, умение выбрать нужный',
             'important', 'N-префикс обязателен для кириллицы',
             'Без N перед строкой (`\'Иванов\'` вместо `N\'Иванов\'`) кириллица '
             'сохранится как вопросительные знаки. Всегда используйте NVARCHAR + N\'...\''),

            (3, 'DDL: создание таблиц с ограничениями',
             'DDL (Data Definition Language) — команды создания структуры БД.\n\n'
             '**Базовый шаблон CREATE TABLE:**\n'
             '```sql\n'
             'CREATE TABLE ИмяТаблицы (\n'
             '    Код         INT           NOT NULL IDENTITY(1,1),\n'
             '    Наименование NVARCHAR(200) NOT NULL,\n'
             '    Дата        DATE          NOT NULL DEFAULT GETDATE(),\n'
             '    Статус      NVARCHAR(20)  NOT NULL DEFAULT N\'новый\'\n'
             '        CONSTRAINT CHK_Статус CHECK (Статус IN (N\'новый\', N\'выполнен\')),\n'
             '    Код_родителя INT          NULL,\n'
             '    CONSTRAINT PK_ИмяТаблицы PRIMARY KEY (Код),\n'
             '    CONSTRAINT FK_К_Родителю FOREIGN KEY (Код_родителя)\n'
             '        REFERENCES Родитель(Код)\n'
             ');\n'
             '```\n\n'
             '**Ключевые ограничения:**\n'
             '• `IDENTITY(1,1)` — автоинкремент начиная с 1, шаг 1\n'
             '• `NOT NULL` / `NULL` — обязательность поля\n'
             '• `DEFAULT` — значение по умолчанию\n'
             '• `CHECK` — проверка допустимых значений\n'
             '• `UNIQUE` — уникальность (для ИНН, логина)\n'
             '• `FOREIGN KEY ... REFERENCES` — связь с другой таблицей\n\n'
             '**Порядок создания важен:** сначала родительские таблицы, потом дочерние.\n'
             'Если таблица B ссылается на A через FK — сначала создать A.',
             'Понимание DDL и умение написать CREATE TABLE с ограничениями',
             'tip', 'Именование ограничений',
             'Давайте ограничениям осмысленные имена: PK_Таблица, FK_Таблица_Родитель, '
             'CHK_Таблица_Поле, UQ_Таблица_Поле. '
             'Это помогает при отладке ошибок.'),

            (4, 'Импорт данных из JSON и заполнение таблиц',
             'На экзамене данные для заполнения таблиц предоставляются в виде файла '
             '(Заказчики.json, данные.xlsx и т.п.).\n\n'
             '**Если данные в JSON — читаем вручную и пишем INSERT:**\n'
             '```sql\n'
             'INSERT INTO КОНТРАГЕНТ (Наименование, ИНН, Город) VALUES\n'
             '(N\'ООО "Пример"\', \'1234567890\', N\'Москва\'),\n'
             '(N\'ИП Иванов\',     \'0987654321\', N\'Казань\');\n'
             '```\n\n'
             '**Если данные в Excel — использовать Import Wizard:**\n'
             '1. Object Explorer → правой кнопкой на БД → Tasks → Import Data\n'
             '2. Data Source: Microsoft Excel → выбрать файл\n'
             '3. Destination: SQL Server Native Client → ваша БД\n'
             '4. Выбрать нужные листы и таблицы → Next → Finish\n\n'
             '**Проверить результат:**\n'
             '```sql\n'
             'SELECT COUNT(*) FROM КОНТРАГЕНТ;        -- количество строк\n'
             'SELECT * FROM КОНТРАГЕНТ;               -- все записи\n'
             'SELECT TOP 5 * FROM КОНТРАГЕНТ;         -- первые 5 строк\n'
             '```',
             'Данные импортированы, SELECT возвращает корректные данные',
             'warning', 'Обязательно проверьте ИНН',
             'ИНН — строка (NVARCHAR), не число! Если хранить как INT — '
             'потеряются ведущие нули в ИНН начинающихся с 0.'),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '2.1 Создание схемы базы данных в СУБД на основании ER-диаграммы',
                '0 — схема не создана или не соответствует ER-диаграмме\n'
                '1 — схема частично соответствует ER-диаграмме (незначительные ошибки)\n'
                '2 — схема полностью соответствует ER-диаграмме, типы данных выбраны верно',
                6,
            ),
            (
                '2.2 Импортирование данных из представленного файла',
                '0 — данные не импортированы\n'
                '1 — данные импортированы частично (незначительные ошибки)\n'
                '2 — данные импортированы полностью',
                6,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_module3(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='3',
            defaults={
                'name': 'Модуль 3: SQL-запросы — основы',
                'description': 'SELECT, JOIN, GROUP BY, агрегатные функции — всё что нужно для написания запроса на экзамене',
                'instruction': (
                    'Этот модуль объясняет базовые конструкции SQL для выборки и обработки данных. '
                    'На экзамене нужно написать один сложный SQL-запрос по заданию. '
                    'Максимальный балл: 5 (критерий 3.1).'
                ),
                'task_condition': (
                    'На экзамене: разработать SQL-запрос к созданной БД в соответствии с заданием. '
                    'Типичное задание: рассчитать стоимость заказов, объединив несколько таблиц, '
                    'с фильтрацией по актуальным ценам.'
                ),
                'duration': '45 минут',
                'max_score': 5,
                'order': 3,
            }
        )

        steps_data = [
            (1, 'SELECT: основы выборки данных',
             'SELECT — основная команда для извлечения данных из таблиц.\n\n'
             '**Базовый синтаксис:**\n'
             '```sql\n'
             'SELECT столбец1, столбец2, ...\n'
             'FROM таблица\n'
             'WHERE условие\n'
             'ORDER BY столбец1 ASC, столбец2 DESC;\n'
             '```\n\n'
             '**Псевдонимы (AS):** делают запрос читаемым\n'
             '```sql\n'
             'SELECT\n'
             '    Наименование  AS Покупатель,\n'
             '    Дата_заказа   AS "Дата заказа",\n'
             '    Количество * Цена AS Сумма\n'
             'FROM ЗАКАЗ_ПОКУПАТЕЛЯ;\n'
             '```\n\n'
             '**WHERE: фильтрация**\n'
             '```sql\n'
             'WHERE Статус = N\'выполнен\'               -- текст\n'
             'WHERE Дата_заказа >= \'2025-01-01\'         -- дата\n'
             'WHERE Сумма BETWEEN 1000 AND 5000         -- диапазон\n'
             'WHERE Город IN (N\'Москва\', N\'Казань\')     -- список\n'
             'WHERE Телефон IS NULL                     -- пустые значения\n'
             '```',
             'Понимание SELECT, WHERE, ORDER BY, псевдонимов',
             None, None, None),

            (2, 'JOIN: объединение таблиц',
             'JOIN объединяет данные из нескольких таблиц по связанным полям.\n\n'
             '**INNER JOIN** — только совпадающие записи (самый частый):\n'
             '```sql\n'
             'SELECT К.Наименование, ЗП.Дата_заказа\n'
             'FROM ЗАКАЗ_ПОКУПАТЕЛЯ ЗП\n'
             'INNER JOIN КОНТРАГЕНТ К ON ЗП.Код_контрагента = К.Код_контрагента;\n'
             '```\n\n'
             '**LEFT JOIN** — все записи из левой таблицы + совпадения из правой:\n'
             '```sql\n'
             'SELECT ЗП.Код_заказа, РС.Итого\n'
             'FROM ЗАКАЗ_ПОКУПАТЕЛЯ ЗП\n'
             'LEFT JOIN РАСЧЕТ_СТОИМОСТИ РС ON ЗП.Код_заказа = РС.Код_заказа;\n'
             '-- Заказы без расчёта стоимости тоже попадут в результат (РС.Итого = NULL)\n'
             '```\n\n'
             '**Цепочка JOIN для нескольких таблиц:**\n'
             '```sql\n'
             'SELECT К.Наименование, Н.Наименование AS Товар, С.Количество, Ц.Цена\n'
             'FROM ЗАКАЗ_ПОКУПАТЕЛЯ ЗП\n'
             'JOIN КОНТРАГЕНТ К      ON ЗП.Код_контрагента  = К.Код_контрагента\n'
             'JOIN СПЕЦИФИКАЦИЯ С    ON ЗП.Код_заказа       = С.Код_заказа\n'
             'JOIN НОМЕНКЛАТУРА Н    ON С.Код_номенклатуры   = Н.Код_номенклатуры\n'
             'JOIN ЦЕНЫ Ц            ON Н.Код_номенклатуры   = Ц.Код_номенклатуры;\n'
             '```\n\n'
             'Правило: JOIN всегда пишется как `JOIN таблица ON условие_связи`.',
             'Понимание INNER JOIN и LEFT JOIN, умение цепочкой объединить 3-4 таблицы',
             'important', 'INNER vs LEFT',
             'INNER JOIN скрывает записи без пары — используйте когда уверены что связь всегда есть. '
             'LEFT JOIN сохраняет все записи левой таблицы — используйте когда связь необязательна.'),

            (3, 'GROUP BY и агрегатные функции',
             'GROUP BY группирует строки, агрегатные функции считают итоги по группам.\n\n'
             '**Агрегатные функции:**\n'
             '| Функция | Что считает |\n'
             '|---|---|\n'
             '| COUNT(*) | Количество строк в группе |\n'
             '| COUNT(поле) | Количество непустых значений |\n'
             '| SUM(поле) | Сумма значений |\n'
             '| AVG(поле) | Среднее значение |\n'
             '| MAX(поле) | Максимальное значение |\n'
             '| MIN(поле) | Минимальное значение |\n\n'
             '**Пример: итоги по каждому заказу**\n'
             '```sql\n'
             'SELECT\n'
             '    ЗП.Код_заказа,\n'
             '    К.Наименование AS Покупатель,\n'
             '    COUNT(С.Код_спецификации) AS Позиций,\n'
             '    SUM(С.Количество * Ц.Цена) AS Итого\n'
             'FROM ЗАКАЗ_ПОКУПАТЕЛЯ ЗП\n'
             'JOIN КОНТРАГЕНТ К   ON ЗП.Код_контрагента = К.Код_контрагента\n'
             'JOIN СПЕЦИФИКАЦИЯ С ON ЗП.Код_заказа = С.Код_заказа\n'
             'JOIN ЦЕНЫ Ц ON С.Код_номенклатуры = Ц.Код_номенклатуры\n'
             'GROUP BY ЗП.Код_заказа, К.Наименование\n'
             'ORDER BY ЗП.Код_заказа;\n'
             '```\n\n'
             '**Правило GROUP BY:** в SELECT можно использовать только:\n'
             '1. Поля из GROUP BY\n'
             '2. Агрегатные функции',
             'Понимание GROUP BY и агрегатных функций',
             'tip', 'HAVING vs WHERE',
             'WHERE фильтрует строки ДО группировки. '
             'HAVING фильтрует группы ПОСЛЕ GROUP BY. '
             'Пример: `HAVING SUM(Сумма) > 10000` — только заказы с итогом больше 10000.'),

            (4, 'Фильтрация по актуальным ценам (типовой паттерн)',
             'На экзамене часто нужно взять цену, актуальную на определённую дату. '
             'Это типовой паттерн — разберите его:\n\n'
             '**Структура таблицы ЦЕНЫ:**\n'
             '```sql\n'
             'Код_номенклатуры  INT  (FK)\n'
             'Цена              DECIMAL(10,2)\n'
             'Дата_начала       DATE   -- с какой даты действует цена\n'
             'Дата_окончания    DATE   -- до какой даты (NULL = актуальна сейчас)\n'
             '```\n\n'
             '**Условие актуальной цены на дату заказа:**\n'
             '```sql\n'
             'JOIN ЦЕНЫ Ц\n'
             '    ON Н.Код_номенклатуры = Ц.Код_номенклатуры\n'
             '    AND Ц.Дата_начала <= ЗП.Дата_заказа\n'
             '    AND (Ц.Дата_окончания IS NULL\n'
             '         OR Ц.Дата_окончания > ЗП.Дата_заказа)\n'
             '```\n\n'
             'Читается: "цена начала действовать ДО даты заказа И либо ещё не закончилась (NULL), '
             'либо закончится ПОСЛЕ даты заказа".\n\n'
             'Без этого условия: если в ЦЕНЫ есть старые и новые цены на один товар — '
             'запрос вернёт задвоенные строки (по одной на каждый период цены).',
             'Понимание паттерна фильтрации актуальных цен по дате',
             'important', 'Самая частая ошибка в Модуле 3',
             'Забыть условие на даты и получить дублирующиеся строки. '
             'Всегда проверяйте: количество строк в результате = '
             'ожидаемому (не больше).'),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '3.1 Создание запроса в соответствии с заданием',
                '0 — запрос отсутствует или не соответствует установленным требованиям\n'
                '1 — запрос частично соответствует установленным требованиям\n'
                '2 — запрос полностью соответствует установленным требованиям',
                5,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_module4(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='4',
            defaults={
                'name': 'Модуль 4: C# Windows Forms — основы',
                'description': 'Архитектура WinForms-приложения, ADO.NET, SHA256, UserControl — всё что нужно знать',
                'instruction': (
                    'Этот модуль объясняет ключевые концепции для разработки приложения на C# WinForms. '
                    'На экзамене нужно создать приложение авторизации с SHA256, '
                    'puzzle-капчей, блокировкой и панелью администратора. '
                    'Максимальный балл: 19.'
                ),
                'task_condition': (
                    'На экзамене: разработать Windows Forms приложение на C# (.NET 6.0) '
                    'для авторизации пользователей с хешированием паролей SHA256, '
                    'puzzle CAPTCHA, блокировкой после 3 ошибок и ролевым доступом.'
                ),
                'duration': '120 минут',
                'max_score': 19,
                'order': 4,
            }
        )

        steps_data = [
            (1, 'Архитектура приложения: зачем делить на классы',
             'Хорошее WinForms-приложение — не один огромный файл Form1.cs, '
             'а набор классов с чёткими обязанностями.\n\n'
             '**Типовая архитектура для экзаменационного приложения:**\n\n'
             '```\n'
             'AuthApp/\n'
             '├── appsettings.json          — настройки (строка подключения)\n'
             '├── Конфигурация.cs           — чтение appsettings.json\n'
             '├── ХешировщикПаролей.cs      — SHA256 хеширование\n'
             '├── МодельПользователя.cs     — POCO: данные одного пользователя\n'
             '├── РепозиторийПользователей.cs — все SQL-запросы к БД\n'
             '├── КомпонентПазл.cs          — UserControl: drag&drop капча\n'
             '├── ФормаВхода.cs             — форма входа\n'
             '├── ФормаАдминистратора.cs    — панель управления\n'
             '├── ДиалогРедактирования.cs   — диалог добавления/изменения\n'
             '├── ФормаПользователя.cs      — приветственный экран\n'
             '└── Program.cs                — точка входа\n'
             '```\n\n'
             '**Принцип единственной ответственности:**\n'
             '• Репозиторий — только SQL, никакой логики форм\n'
             '• Формы — только UI и реакция на события, никаких SQL-запросов напрямую\n'
             '• Модель — только данные, никакой логики\n\n'
             'Такое разделение упрощает отладку: если ошибка в SQL — смотришь только в Репозиторий.',
             'Понимание архитектуры приложения и роли каждого класса',
             'tip', 'Начинайте с Репозитория',
             'Напишите и протестируйте все SQL-методы репозитория ДО того как рисовать формы. '
             'Так легче отлаживать — можно вызвать метод из Program.cs и проверить в консоли.'),

            (2, 'ADO.NET: подключение к SQL Server',
             'ADO.NET — стандартный способ работы с БД в .NET без ORM.\n\n'
             '**Базовый паттерн для каждого метода:**\n'
             '```csharp\n'
             'using Microsoft.Data.SqlClient;\n\n'
             'private static string _conn = "Server=.;Database=ВашаБД;Integrated Security=true;TrustServerCertificate=true;";\n\n'
             '// Чтение данных\n'
             'public static МодельПользователя? НайтиПоЛогину(string логин)\n'
             '{\n'
             '    using var conn = new SqlConnection(_conn);\n'
             '    conn.Open();\n'
             '    using var cmd = new SqlCommand(\n'
             '        "SELECT Код,Логин,Роль FROM Пользователи WHERE Логин=@л", conn);\n'
             '    cmd.Parameters.AddWithValue("@л", логин);  // защита от SQL-инъекции!\n'
             '    using var reader = cmd.ExecuteReader();\n'
             '    if (!reader.Read()) return null;\n'
             '    return new МодельПользователя { Код = reader.GetInt32(0), ... };\n'
             '}\n\n'
             '// Запись данных (INSERT/UPDATE)\n'
             'public static void Добавить(string логин, string хеш)\n'
             '{\n'
             '    using var conn = new SqlConnection(_conn);\n'
             '    conn.Open();\n'
             '    using var cmd = new SqlCommand(\n'
             '        "INSERT INTO Пользователи(Логин,Хеш_пароля) VALUES(@л,@х)", conn);\n'
             '    cmd.Parameters.AddWithValue("@л", логин);\n'
             '    cmd.Parameters.AddWithValue("@х", хеш);\n'
             '    cmd.ExecuteNonQuery();\n'
             '}\n'
             '```\n\n'
             '**using** закрывает соединение автоматически — никогда не забывайте его.',
             'Понимание паттерна ADO.NET: SqlConnection → SqlCommand → Parameters → Execute',
             'important', 'Параметры — не конкатенация строк',
             'НИКОГДА не пишите `"WHERE Логин=\'" + логин + "\'"` — это SQL-инъекция. '
             'ВСЕГДА используйте Parameters.AddWithValue("@параметр", значение).'),

            (3, 'SHA256: хеширование паролей',
             'Пароли нельзя хранить в открытом виде — только в виде хеша.\n\n'
             '**Как работает SHA256:**\n'
             '1. Берёт строку пароля\n'
             '2. Применяет алгоритм SHA256 → получает 32 байта (256 бит)\n'
             '3. Конвертирует в hex-строку из 64 символов\n\n'
             '**Реализация в C#:**\n'
             '```csharp\n'
             'using System.Security.Cryptography;\n'
             'using System.Text;\n\n'
             'public static string Хешировать(string пароль)\n'
             '{\n'
             '    using var sha = SHA256.Create();\n'
             '    byte[] bytes = sha.ComputeHash(Encoding.UTF8.GetBytes(пароль));\n'
             '    return BitConverter.ToString(bytes).Replace("-", "").ToLower();\n'
             '}\n'
             '```\n\n'
             '**Проверка пароля при входе:**\n'
             '```csharp\n'
             'string введённыйХеш = ХешировщикПаролей.Хешировать(txtПароль.Text);\n'
             'bool верный = (введённыйХеш == пользователь.Хеш_пароля);\n'
             '```\n\n'
             '**Известные хеши для тестовых данных:**\n'
             '• SHA256("admin") = `8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918`\n'
             '• SHA256("user")  = `04f8996da763b7a969b1028ee3007569eaf3a635486ddab211d512c85b9df8fb`',
             'Понимание принципа хеширования и реализация метода Хешировать',
             'info', 'SHA256 необратим',
             'Зная хеш, нельзя восстановить исходный пароль. '
             'Поэтому при "смене пароля" нужно заново ввести пароль, '
             'захешировать и сохранить новый хеш.'),

            (4, 'UserControl: создание компонента КомпонентПазл',
             'UserControl — переиспользуемый графический компонент. '
             'Создаётся как обычная форма, но без рамки — её добавляют на другие формы.\n\n'
             '**Создать UserControl:**\n'
             'Project → Add → User Control → имя КомпонентПазл.cs\n\n'
             '**Логика puzzle-капчи:**\n'
             '```csharp\n'
             'public partial class КомпонентПазл : UserControl\n'
             '{\n'
             '    // Массив: правильный порядок фрагментов\n'
             '    private int[] _порядок = { 0, 1, 2, 3 };\n'
             '    private List<Image> _фрагменты = new();\n\n'
             '    // Свойство: собрана ли капча?\n'
             '    public bool ЯСобран =>\n'
             '        _порядок[0]==0 && _порядок[1]==1 &&\n'
             '        _порядок[2]==2 && _порядок[3]==3;\n\n'
             '    // Перемешать фрагменты\n'
             '    public void Перемешать()\n'
             '    {\n'
             '        _порядок = _порядок.OrderBy(_ => Random.Shared.Next()).ToArray();\n'
             '        Invalidate(); // перерисовать\n'
             '    }\n'
             '}\n'
             '```\n\n'
             '**Drag & Drop (упрощённый вариант):**\n'
             '• OnMouseDown → запомнить какой фрагмент захвачен (_захваченный = индекс)\n'
             '• OnMouseUp → определить куда брошен → поменять местами в _порядок[] → Invalidate()\n'
             '• OnPaint → нарисовать 4 фрагмента в текущем _порядок[]\n\n'
             'После добавления в проект и сборки (Ctrl+Shift+B) '
             'компонент появится в Toolbox и его можно перетащить на форму.',
             'Понимание концепции UserControl и базовой логики puzzle-капчи',
             None, None, None),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '4.1 Разработка функционала — форма авторизации',
                '0 — форма авторизации не выполнена\n'
                '1 — форма выполнена частично, ниже требований\n'
                '2 — форма выполнена полностью согласно требованиям',
                2,
            ),
            (
                '4.2 Разработка функционала — блокировка пользователя',
                '0 — блокировка не реализована\n'
                '1 — блокировка реализована частично (незначительные ошибки)\n'
                '2 — блокировка реализована полностью: счётчик ошибок, автоблокировка при 3 попытках',
                4,
            ),
            (
                '4.3 Разработка функционала — смарт-капча',
                '0 — капча не реализована\n'
                '1 — капча реализована частично (незначительные ошибки)\n'
                '2 — puzzle-капча из 4 фрагментов реализована полностью (drag & drop, проверка сборки)',
                4,
            ),
            (
                '4.4 Разработка графического интерфейса в соответствии с требованиями',
                '0 — интерфейс не соответствует требованиям к разработке\n'
                '1 — интерфейс частично соответствует требованиям\n'
                '2 — интерфейс полностью соответствует требованиям к разработке',
                2,
            ),
            (
                '4.5 Функционал администратора — добавление пользователей',
                '0 — добавление не реализовано\n'
                '1 — добавление реализовано частично\n'
                '2 — добавление пользователей работает полностью',
                2,
            ),
            (
                '4.6 Функционал администратора — изменение данных пользователей',
                '0 — изменение не реализовано\n'
                '1 — изменение реализовано частично\n'
                '2 — изменение данных пользователей работает полностью',
                2,
            ),
            (
                '4.7 Функционал администратора — контроль уникальности',
                '0 — контроль уникальности не реализован\n'
                '1 — контроль уникальности реализован частично\n'
                '2 — уникальность логина проверяется (UNIQUE в БД + проверка в коде)',
                2,
            ),
            (
                '4.8 Выделение наиболее значимых блоков информации для решения задач',
                '0 — задачи не решены\n'
                '1 — задачи решены наполовину\n'
                '2 — задачи полностью решены',
                1,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_module5(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='5',
            defaults={
                'name': 'Модуль 5: Проектная документация — основы',
                'description': 'Как оформить проектную документацию: назначение системы и описание методов с параметрами',
                'instruction': (
                    'Этот модуль объясняет что должна содержать проектная документация на экзамене. '
                    'Документ оформляется в MS Word. '
                    'Максимальный балл: 6 (критерий 5.1).'
                ),
                'task_condition': (
                    'На экзамене: создать проектную документацию для приложения из Модуля 4. '
                    'Документ должен содержать описание функционального назначения '
                    'и перечень используемых методов с указанием параметров.'
                ),
                'duration': '60 минут',
                'max_score': 6,
                'order': 5,
            }
        )

        steps_data = [
            (1, 'Структура проектной документации',
             'Проектная документация — официальный документ, описывающий разработанную систему.\n\n'
             '**Обязательные разделы для экзамена:**\n\n'
             '**1. Функциональное назначение системы**\n'
             'Ответьте на вопросы:\n'
             '• Что делает система? (авторизация, управление данными, расчёты)\n'
             '• Для кого? (сотрудники предприятия, администраторы)\n'
             '• Какие задачи решает? (защита от несанкционированного доступа, ролевое разграничение)\n'
             '• Какие технологии используются? (C# .NET 6.0, MSSQL 2019, SHA256)\n\n'
             '**2. Используемые методы с параметрами**\n'
             'Таблица со всеми публичными методами всех классов приложения:\n'
             '• Класс\n'
             '• Имя метода\n'
             '• Параметры (имя и тип каждого)\n'
             '• Тип возвращаемого значения\n'
             '• Краткое описание\n\n'
             '**3. Скриншоты экранных форм**\n'
             'Минимум 3 скриншота с подписями: "Рисунок 1 — Форма входа" и т.д.',
             'Понимание структуры документа: назначение, методы, скриншоты',
             'important', 'Критерий 5.1 — именно методы с параметрами',
             'Самая частая ошибка: написать только описание системы, забыв таблицу методов. '
             'Критерий 5.1 явно требует "используемые методы с указанием параметров".'),

            (2, 'Как описывать методы: таблица с параметрами',
             'Это ключевая часть документа. Оформите в виде таблицы:\n\n'
             '**Пример таблицы методов:**\n\n'
             '| Класс | Метод | Параметры | Возвращает | Описание |\n'
             '|---|---|---|---|---|\n'
             '| ХешировщикПаролей | Хешировать | string пароль | string | SHA256-хеш в нижнем регистре |\n'
             '| РепозиторийПользователей | НайтиПоЛогину | string логин | МодельПользователя? | Поиск по логину |\n'
             '| РепозиторийПользователей | Авторизовать | string логин, string хешПароля | bool | Проверка логина и пароля |\n'
             '| РепозиторийПользователей | ОбновитьОшибки | int кодПользователя | void | +1 к счётчику ошибок |\n'
             '| РепозиторийПользователей | Разблокировать | int кодПользователя | void | Снять блокировку |\n'
             '| РепозиторийПользователей | Добавить | string логин, string хеш, string роль | void | Новый пользователь |\n'
             '| РепозиторийПользователей | Изменить | МодельПользователя модель | void | Обновить данные |\n'
             '| РепозиторийПользователей | ПолучитьВсех | — | List<МодельПользователя> | Все пользователи |\n'
             '| КомпонентПазл | Перемешать | — | void | Случайно расставить фрагменты |\n'
             '| КомпонентПазл | ЯСобран | — (свойство) | bool | Проверка правильной сборки |\n\n'
             'Не забудьте: тип void = метод ничего не возвращает; ? = может быть null.',
             'Таблица методов составлена для всех публичных методов приложения',
             'tip', 'Скопируйте сигнатуры из кода',
             'Откройте каждый .cs файл, найдите строки `public static` или `public` '
             'и скопируйте имя метода и параметры прямо оттуда. '
             'Так не пропустите ни одного метода.'),

            (3, 'Скриншоты и оформление Word-документа',
             'Документ должен выглядеть профессионально. Несколько правил:\n\n'
             '**Заголовки:** используйте стили Word (Заголовок 1, Заголовок 2) — '
             'так автоматически строится оглавление.\n\n'
             '**Таблицы:** Вставить → Таблица → задать нужное количество строк и столбцов. '
             'Шапку таблицы выделить жирным.\n\n'
             '**Скриншоты:**\n'
             '1. Запустить приложение (нажать F5 в Visual Studio)\n'
             '2. Сделать скриншот нужной формы: Win+Shift+S или Alt+PrintScreen\n'
             '3. Вставить в документ: Ctrl+V\n'
             '4. Подписать: "Рисунок N — Название формы"\n\n'
             '**Обязательные скриншоты:**\n'
             '• Форма входа с puzzle-капчей\n'
             '• Форма с сообщением об ошибке\n'
             '• Панель администратора с ListView\n'
             '• Форма пользователя (приветствие)\n\n'
             '**Сохранение:** Ctrl+S → формат .docx\n'
             'Имя файла: Проектная_документация_AuthApp.docx',
             'Документ оформлен со скриншотами, сохранён в .docx',
             None, None, None),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '5.1 Создание проектной документации с описанием функционального назначения и методов',
                '0 — документация отсутствует или файл пустой\n'
                '1 — документация содержит описание функционального назначения и методов с параметрами, но с ошибками\n'
                '2 — документация содержит описание функционального назначения, используемые методы с указанием параметров',
                6,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_module6(self, section):
        module, _ = Module.objects.update_or_create(
            section=section,
            task_number='6',
            defaults={
                'name': 'Модуль 6: Web-приложение — основы',
                'description': 'HTML, CSS, JavaScript fetch() — как написать форму валидации СНИЛС с нуля',
                'instruction': (
                    'Этот модуль объясняет что нужно знать для написания веб-приложения. '
                    'На экзамене нужно самостоятельно создать HTML-файл, '
                    'написать два метода валидации и обработать ответ API. '
                    'Максимальный балл: 25.'
                ),
                'task_condition': (
                    'На экзамене: написать snils_app.html — веб-форму для валидации СНИЛС. '
                    'Приложение должно: принимать СНИЛС, отправлять GET-запрос к API эмулятора, '
                    'отображать результат на форме, обрабатывать ошибки через try/catch, '
                    'заполнять тестовый документ.'
                ),
                'duration': '60 минут',
                'max_score': 25,
                'order': 6,
            }
        )

        steps_data = [
            (1, 'Структура HTML5-документа',
             'Любая веб-страница начинается с одной и той же структуры:\n\n'
             '```html\n'
             '<!DOCTYPE html>\n'
             '<html lang="ru">\n'
             '<head>\n'
             '    <meta charset="UTF-8">\n'
             '    <title>Заголовок вкладки</title>\n'
             '    <style>\n'
             '        /* CSS-стили */\n'
             '    </style>\n'
             '</head>\n'
             '<body>\n'
             '    <!-- HTML-содержимое -->\n'
             '    <script>\n'
             '        // JavaScript-код\n'
             '    </script>\n'
             '</body>\n'
             '</html>\n'
             '```\n\n'
             '**Ключевые теги для формы валидации:**\n'
             '| Тег | Назначение |\n'
             '|---|---|\n'
             '| `<input type="text">` | Поле ввода текста |\n'
             '| `<button onclick="fn()">` | Кнопка, вызывающая функцию |\n'
             '| `<div id="result">` | Блок для вывода результата |\n'
             '| `<label for="id">` | Подпись к полю |\n\n'
             '**В VS Code:** создать файл .html, ввести `!` и нажать Tab — '
             'автоматически вставится базовая заготовка.',
             'Понимание структуры HTML5: head, body, style, script',
             'tip', 'id — мост между HTML и JavaScript',
             '`id="snilsInput"` в HTML и `document.getElementById("snilsInput")` в JS — '
             'это один и тот же элемент. Без id JS не сможет найти поле ввода.'),

            (2, 'CSS: базовая стилизация формы',
             'CSS задаёт внешний вид элементов. Для экзамена достаточно простых стилей:\n\n'
             '```css\n'
             'body {\n'
             '    font-family: Arial, sans-serif;\n'
             '    display: flex;\n'
             '    justify-content: center;\n'
             '    align-items: center;\n'
             '    min-height: 100vh;\n'
             '    background: #f0f2f5;\n'
             '}\n'
             '.container {\n'
             '    background: white;\n'
             '    padding: 40px;\n'
             '    border-radius: 12px;\n'
             '    box-shadow: 0 4px 20px rgba(0,0,0,0.1);\n'
             '    width: 400px;\n'
             '}\n'
             'input, button { width: 100%; padding: 10px; margin-bottom: 12px; }\n'
             '/* Цветовая индикация результата */\n'
             '#result.success { background: #e8f5e9; color: #2e7d32; padding: 12px; border-radius: 6px; }\n'
             '#result.error   { background: #ffebee; color: #c62828; padding: 12px; border-radius: 6px; }\n'
             '```\n\n'
             '**Ключевые свойства:**\n'
             '• `display: flex; justify-content: center; align-items: center; min-height: 100vh` — '
             'центрирует карточку на экране\n'
             '• `box-shadow` — тень для эффекта "карточки"\n'
             '• Классы `.success` и `.error` переключаются из JavaScript',
             'CSS добавлен: форма по центру, цветовая индикация результата',
             None, None, None),

            (3, 'JavaScript: два метода валидации',
             'На экзамене требуется ДВА метода валидации (критерий 6.1):\n\n'
             '**Метод 1 — локальная проверка формата:**\n'
             'Проверяет что введено ровно 11 цифр, ещё до обращения к API.\n'
             '```javascript\n'
             'function validateLocal(value) {\n'
             '    const digits = value.replace(/[\\s\\-]/g, "");\n'
             '    if (!/^\\d{11}$/.test(digits)) {\n'
             '        throw new Error("СНИЛС должен содержать 11 цифр");\n'
             '    }\n'
             '    return digits;\n'
             '}\n'
             '```\n\n'
             '**Метод 2 — проверка через API:**\n'
             'Отправляет запрос к эмулятору и получает форматированный СНИЛС.\n'
             '```javascript\n'
             'async function validateApi(digits) {\n'
             '    const url = `http://localhost:3000/api/snils?value=${encodeURIComponent(digits)}`;\n'
             '    const response = await fetch(url);\n'
             '    if (!response.ok) throw new Error("Ошибка API: " + response.status);\n'
             '    const data = await response.json();\n'
             '    return data.value; // "890-123-456 78"\n'
             '}\n'
             '```\n\n'
             '**Ключевые слова async/await:**\n'
             '• `async function` — функция может ждать ответа от сети\n'
             '• `await fetch(url)` — ждёт ответа сервера, не блокируя страницу\n'
             '• `await response.json()` — парсит тело ответа как JSON',
             'Написаны два метода валидации: локальный и через API',
             'important', 'Уточните порт эмулятора у экзаменатора',
             'Порт 3000 — типичное значение, но может отличаться. '
             'На экзамене спросите адрес API у экзаменатора или найдите в файле '
             '"Настройка ПК для эмулятора.pdf".'),

            (4, 'try/catch и отображение результата',
             'try/catch — обязательная обработка ошибок (критерий 6.4).\n\n'
             '**Главная функция с обработкой ошибок:**\n'
             '```javascript\n'
             'function showResult(message, isSuccess) {\n'
             '    const div = document.getElementById("result");\n'
             '    div.textContent = message;\n'
             '    div.className = isSuccess ? "success" : "error";\n'
             '    div.style.display = "block";\n'
             '}\n\n'
             'async function validateSnils() {\n'
             '    const input = document.getElementById("snilsInput").value.trim();\n'
             '    if (!input) {\n'
             '        showResult("Введите СНИЛС", false);\n'
             '        return;\n'
             '    }\n'
             '    try {\n'
             '        const digits = validateLocal(input);          // метод 1\n'
             '        const formatted = await validateApi(digits);  // метод 2\n'
             '        showResult("СНИЛС: " + formatted, true);      // успех\n'
             '    } catch (err) {\n'
             '        showResult("Ошибка: " + err.message, false);  // любая ошибка\n'
             '    }\n'
             '}\n'
             '```\n\n'
             '**Почему try/catch важен:**\n'
             'Без него: ошибка сети → страница "падает" → белый экран или ошибка в консоли.\n'
             'С try/catch: ошибка перехватывается → пользователь видит понятное сообщение.\n\n'
             '**Запуск через start_chrome.bat:**\n'
             '```bat\n'
             '@echo off\n'
             'start chrome.exe --allow-file-access-from-files --disable-web-security '
             '--user-data-dir="%TEMP%\\chrome_snils" "%~dp0snils_app.html"\n'
             '```\n'
             'Флаг `--disable-web-security` нужен чтобы браузер разрешил fetch() из локального файла.',
             'Написана главная функция с try/catch, showResult, start_chrome.bat',
             'tip', 'Тестируйте по порядку',
             '1) Сначала проверьте пустое поле, 2) введите неверный формат, '
             '3) введите правильный СНИЛС. '
             'Так убедитесь что оба метода валидации и обработка ошибок работают.'),
        ]

        self._create_steps(module, steps_data)

        criteria = [
            (
                '6.1 Написание методов для валидации на корректность данных',
                '0 — методы валидации не разработаны\n'
                '1 — разработан один метод валидации согласно заполненному документу\n'
                '2 — разработаны два метода валидации согласно заполненному документу',
                5,
            ),
            (
                '6.2 Приём данных с эмулятора и размещение результата на форме',
                '0 — функционал не выполнен\n'
                '1 — функционал выполнен частично (незначительные ошибки)\n'
                '2 — данные принимаются с эмулятора и результат размещается на форме полностью',
                6,
            ),
            (
                '6.3 Размещение результата проверки на форме и в столбце "Результат" документа',
                '0 — результат не размещён\n'
                '1 — результат размещён частично (незначительные ошибки)\n'
                '2 — результат полностью отображается на форме и записывается в столбец "Результат"',
                6,
            ),
            (
                '6.4 Использование отладки и обработки исключительных ситуаций',
                '0 — приложение аварийно завершается, обработки исключений нет\n'
                '1 — приложение не завершается аварийно, но обработки исключений в коде нет\n'
                '2 — приложение не завершается аварийно, код содержит try/catch для обработки исключений',
                4,
            ),
            (
                '6.5 Заполнение документа: столбцы "Действие", "Ожидаемый результат" и разметка столбца "Результат"',
                '0 — документ не заполнен и не размечен\n'
                '1 — документ заполнен и размечен частично (незначительные ошибки)\n'
                '2 — документ заполнен и размечен полностью: все столбцы и закладки/разметки добавлены',
                4,
            ),
        ]
        self._create_criteria(module, criteria)

    def _create_steps(self, module, steps_data):
        for row in steps_data:
            number, title, description, expected_result, note_type, note_title, note_content = row
            step, _ = ModuleStep.objects.update_or_create(
                module=module,
                number=number,
                defaults={
                    'title': title,
                    'description': description,
                    'expected_result': expected_result or '',
                    'order': number,
                }
            )
            if note_type and note_content:
                StepNote.objects.update_or_create(
                    step=step,
                    title=note_title or '',
                    defaults={
                        'note_type': note_type,
                        'content': note_content,
                        'order': 1,
                    }
                )

    def _create_criteria(self, module, criteria):
        for order, (name, description, score) in enumerate(criteria, start=1):
            EvaluationCriteria.objects.update_or_create(
                module=module,
                name=name,
                defaults={
                    'description': description,
                    'max_score': score,
                    'order': order,
                }
            )
